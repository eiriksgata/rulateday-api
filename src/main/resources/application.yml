knife4j:
  enable: true
  production: false
  documents:
    - group: 1.0.0
      name: 其他功能说明
      # 某一个文件夹下所有的.md文件
      locations: classpath:markdown/*

spring:
  application:
    name: rulateday-api

  datasource:
    url: jdbc:sqlite:data/rulateday-api.db
    username:
    password:


  profiles:
    active: config
    swagger: true
    authorization:
      username: root
      password: admin&admin
      enable: true
    cores: false

  main:
    allow-bean-definition-overriding: true

jwt:
  header: Authorization
  expire: 604800
  secret: bf8390759d67d4ea58383a2103060c50a7852e0eb430a7ff1b0691ad78c31bdf

pagehelper:
  helperDialect: sqlite
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql